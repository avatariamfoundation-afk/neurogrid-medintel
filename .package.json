{
  "name": "medintel",
  "version": "1.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network testnet"
  },
  "dependencies": {
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "dotenv": "^16.0.0",
    "ethers": "^5.7.0",
    "crypto-js": "^4.1.1"  // Added for basic encryption
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "circom": "^2.1.0"  // Added for ZKP privacy
  }
}
