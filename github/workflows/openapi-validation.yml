name: OpenAPI Validation

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate OpenAPI
        run: |
          npm install -g @redocly/cli
          find . -name "openapi.yaml" -exec redocly lint {} \;

